{
  "name": "builderfox",
  "version": "1.0.0",
  "private": true,
  "description": "Brand site for BuilderFox, a creative agency that builds and publishes smart, tactical games. First title in production: Wrestling City Tactics, a turn-based tactics RPG where the ring meets the grid.",
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AaronBergquist/builderfox.git"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@11ty/eleventy-plugin-rss": "^3.0.0"
  }
}
